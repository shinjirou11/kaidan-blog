---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="お問い合わせ"
  description="怪談と闇の記録へのお問い合わせはこちらから。記事に関するご意見・ご感想もお気軽にどうぞ。"
>
  <div class="container">
    <div class="static-page">
      <header class="static-page__header">
        <p class="static-page__label">Contact</p>
        <h1 class="static-page__title">お問い合わせ</h1>
        <p class="static-page__desc">
          記事に関するご意見・ご感想、その他お気軽にどうぞ。<br />
          通常、数日以内にご返信いたします。
        </p>
      </header>

      <form
        class="contact-form"
        action="mailto:m97769637@gmail.com"
        method="POST"
        enctype="text/plain"
      >
        <div class="contact-form__field">
          <label class="contact-form__label" for="name">お名前</label>
          <input
            class="contact-form__input"
            type="text"
            id="name"
            name="name"
            required
            placeholder="山田 太郎"
          />
        </div>

        <div class="contact-form__field">
          <label class="contact-form__label" for="email">メールアドレス</label>
          <input
            class="contact-form__input"
            type="email"
            id="email"
            name="email"
            required
            placeholder="example@email.com"
          />
        </div>

        <div class="contact-form__field">
          <label class="contact-form__label" for="subject">件名</label>
          <input
            class="contact-form__input"
            type="text"
            id="subject"
            name="subject"
            placeholder="記事へのご感想など"
          />
        </div>

        <div class="contact-form__field">
          <label class="contact-form__label" for="message">メッセージ <span class="contact-form__required">*</span></label>
          <textarea
            class="contact-form__textarea"
            id="message"
            name="message"
            rows="7"
            required
            placeholder="メッセージを入力してください"
          ></textarea>
        </div>

        <button class="contact-form__submit" type="submit">送信する</button>
      </form>

      <p class="contact-form__note">
        ※ 送信ボタンを押すとメーラーが開きます。
      </p>
    </div>
  </div>
</BaseLayout>

<style>
  .static-page {
    padding: var(--space-lg) 0 var(--space-xl);
    max-width: 560px;
  }

  .static-page__header {
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-md);
    border-bottom: 1px solid var(--border);
  }

  .static-page__label {
    font-family: var(--font-sans);
    font-size: 0.65rem;
    font-weight: 300;
    letter-spacing: 0.3em;
    color: var(--text-muted);
    text-transform: uppercase;
    margin-bottom: 0.6rem;
  }

  .static-page__title {
    font-size: clamp(1.6rem, 4vw, 2.2rem);
    font-weight: 300;
    letter-spacing: 0.06em;
    margin-bottom: var(--space-xs);
  }

  .static-page__desc {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 300;
    color: var(--text-secondary);
    line-height: 1.8;
    margin-top: var(--space-xs);
  }

  /* Form */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .contact-form__field {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .contact-form__label {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 300;
    letter-spacing: 0.1em;
    color: var(--text-secondary);
  }

  .contact-form__required {
    color: var(--accent-light);
  }

  .contact-form__input,
  .contact-form__textarea {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-light);
    color: var(--text-primary);
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 300;
    padding: 0.7em 0.9em;
    width: 100%;
    outline: none;
    transition: border-color 0.2s;
    -webkit-appearance: none;
    border-radius: 0;
  }

  .contact-form__input::placeholder,
  .contact-form__textarea::placeholder {
    color: var(--text-muted);
  }

  .contact-form__input:focus,
  .contact-form__textarea:focus {
    border-color: var(--text-muted);
  }

  .contact-form__textarea {
    resize: vertical;
    min-height: 160px;
    line-height: 1.7;
  }

  .contact-form__submit {
    align-self: flex-start;
    background: none;
    border: 1px solid var(--border-light);
    color: var(--text-secondary);
    cursor: pointer;
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 300;
    letter-spacing: 0.15em;
    padding: 0.7em 2em;
    margin-top: var(--space-xs);
    transition: all 0.2s;
  }

  .contact-form__submit:hover {
    border-color: var(--text-muted);
    color: var(--text-primary);
  }

  .contact-form__note {
    font-family: var(--font-sans);
    font-size: 0.7rem;
    font-weight: 300;
    color: var(--text-muted);
    margin-top: var(--space-md);
  }

  .contact-form__note a {
    color: var(--text-muted);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
</style>
